#AI支援ナビ実装メモ

# AI支援ナビ機能 実装メモ

## 1. 現在の研究アプリの画面構成

・ログイン画面
・基本情報
・現在の状態
・対応内容
・支援者の状態
・保存画面

## 2. AI支援ナビ機能を入れる画面

AI支援ナビ機能は、「対応内容」画面に組み込む。

理由：
支援者が、支援される側の現在の状態を確認した後に、どのように対応すればよいかを考える場面で参考情報を提示するため。

## 3. 最初に実装すること

まずは、「対応内容」画面に困りごとのカテゴリ選択を追加する。

## 4. 実装方針

最初から研究アプリ本体の流れを大きく変更しない。
まずは、対応内容画面の中に小さくカテゴリ選択を追加し、選択したカテゴリに応じて対応例を表示できるようにする。

## 5. 実装順序

1. 対応内容画面のファイルを確認する
2. 困りごとのカテゴリ選択を追加する
3. 選択したカテゴリに応じた対応例を表示する
4. 画面表示を確認する
5. 問題がなければ保存処理との連携を検討する
